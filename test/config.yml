#Input section 
input:
  #input fasta file name
  fasta: IWGSC_v1.1_HC_20170706_pep_only_longest_splice_variant.fa
  # output file basename
  basename: IWGSC_v1.1_HC_20170706-mpi
  #input NCBI taxonomy id
  taxon: "4577"
  # Name of the species
  species: "Zea mays"
  # Email is mandatory
  email: 
  #Number of CPUs used for tools
  cpus: 4
  #Whether openmpi should be used
  mpi: False
  #what the name of the temporary directory is
  tmpdir: "/tmpdir"
  num_seqs: 30
  small_seqs: 30

